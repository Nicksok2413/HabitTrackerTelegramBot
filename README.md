# 🚀 Habit Tracker Telegram Bot

![Python](https://img.shields.io/badge/Python-3.12-blue?style=for-the-badge&logo=python&logoColor=white)
![FastAPI](https://img.shields.io/badge/FastAPI-0.121.1-009688?style=for-the-badge&logo=fastapi&logoColor=white)
![Aiogram](https://img.shields.io/badge/Aiogram-3.22-2CA5E0?style=for-the-badge&logo=telegram&logoColor=white)
![PostgreSQL](https://img.shields.io/badge/PostgreSQL-18-336791?style=for-the-badge&logo=postgresql&logoColor=white)
![Docker](https://img.shields.io/badge/Docker-Compose-2496ED?style=for-the-badge&logo=docker&logoColor=white)
![CI](https://github.com/Nicksok2413/HabitTrackerTelegramBot/actions/workflows/ci.yml/badge.svg)

Современный, асинхронный сервис для трекинга привычек через Telegram.

---

## ✨ Основной функционал

*   **⚡️ Управление привычками:** Создание, редактирование, удаление и просмотр списка привычек.
*   **📅 Трекинг выполнения:** Отметка выполнения "на сегодня", поддержка статусов (выполнено/пропущено).
*   **🔥 Система стриков (Streaks):** Подсчет непрерывных серий выполнений. Автоматический сброс стрика при пропуске дня.
*   **🌍 Умные уведомления:** Персональные напоминания в заданное время с учетом **часового пояса** пользователя.
*   **🏆 Геймификация:** Автоматическое завершение привычки при достижении цели (например, 21 день).
*   **📱 Удобный UI:** Inline-кнопки, пагинация списков, FSM (машина состояний) для диалогов.

---

## 🛠 Технологический стек

### Core & Infrastructure
*   **Python 3.12** — современный синтаксис и типизация.
*   **Docker & Docker Compose** — полная контейнеризация всех компонентов. Единый `Dockerfile` (Multi-stage build).
*   **PostgreSQL 18** — основная база данных.
*   **Make** — автоматизация рутинных команд.
*   **Poetry** — управление зависимостями.

### Backend (API)
*   **FastAPI** — высокопроизводительный REST API.
*   **SQLAlchemy 2.0 (Async)** — работа с БД (ORM).
*   **Alembic** — управление миграциями схемы БД.
*   **Pydantic v2** — валидация данных и схемы.
*   **PyJWT** — аутентификация через JWT токены.

### Frontend (Bot)
*   **Aiogram 3.22** — полностью асинхронный фреймворк для Telegram API.
*   **FSM (Finite State Machine)** — управление состояниями диалогов.
*   **Httpx** — асинхронные запросы к Backend API.

### Scheduler (Планировщик)
*   **Apscheduler** — выполнение периодических задач (отправка уведомлений, обслуживание БД).
*   **Timezone Aware** — корректная работа с часовыми поясами через `zoneinfo`.

### Quality Assurance (QA)
*   **Pytest** — изолированная БД в Docker-контейнере (docker-compose.test.yml).
*   **Ruff** — быстрый линтер и форматтер.
*   **Mypy** — строгая статическая типизация (`strict mode`).
*   **GitHub Actions** — CI пайплайн для автоматической проверки кода.

---

## 🏛 Архитектура

Проект разделен на 3 независимых сервиса, работающих в одной сети:

1.  **API Service:** Ядро системы. Реализует бизнес-логику, работает с БД, предоставляет REST API. Использует паттерны *Repository* и *Unit of Work*.
2.  **Bot Service:** "Тонкий клиент". Отвечает только за взаимодействие с пользователем (UI/UX). Общается с данными только через API.
3.  **Scheduler Service:** Фоновый воркер. Отправляет уведомления и выполняет задачи обслуживания (maintenance jobs). Работает с БД через модели и репозитории API.

---

## 🚀 Быстрый старт

### Предварительные требования
*   Docker
*   make (опционально)

### Установка и запуск

1.  **Клонируйте репозиторий:**
    ```bash
    git clone https://github.com/Nicksok2413/HabitTrackerTelegramBot.git
    cd HabitTrackerTelegramBot
    ```

2.  **Настройте окружение:**
    Скопируйте пример файла конфигурации и заполните его своими данными (Токен бота, пароли):
    ```bash
    cp .env.example .env
    ```
    > ⚠️ **Важно:** Обязательно сгенерируйте надежные `DB_PASSWORD`, `API_BOT_SHARED_KEY` и `JWT_SECRET_KEY`.

3.  **Запустите проект:**
    Используйте make для быстрой сборки и запуска:
    ```bash
    make up
    ```
    *Или классическим способом: `docker compose up -d --build`*

4.  **Примените миграции:**
    ```bash
    make migrate
    ```
    *Или: `docker compose run --rm api-migrate`*

5.  **Готово!**
    Бот доступен в Telegram. API доступно по адресу `http://localhost:8000/docs` (`http://0.0.0.0:8000/docs`).

---

## 👨‍💻 Команды разработки (Makefile)

В проекте настроен `Makefile` для удобства разработки:

---
*   `make install` — Установить все зависимости.
---
*   `make up` — Собрать и запустить контейнеры в фоне.
*   `make down` — Остановить контейнеры.
*   `make rebuild` — Пересобрать контейнеры.
*   `make prune` — Остановить контейнеры и УДАЛИТЬ ВСЕ ДАННЫЕ (БД, логи).
*   `make logs` — Просмотр логов всех сервисов в реальном времени.
---
*   `make migrate` — Применить миграции к БД.
*   `make revision m="message"` — Создать новую миграцию (автогенерация).
---
*   `make lint` — Проверить код линтером Ruff.
*   `make lint-fix` — Исправить код линтером Ruff.
*   `make format-check` — Проверить код форматтером Ruff.
*   `make format` — Отформатировать код форматтером Ruff.
*   `make types` — Проверить статическую типизацию mypy.
*   `make test` — Запустить тесты.
*   `make test-cov` — Запустить тесты с отчетом о покрытии (`coverage`).
*   `make check` — **Запустить статический анализ** (lint, format-check, types).
*   `make check-all` — **Запустить полный цикл проверок** (check, test).


---

## 🧪 Тестирование

Для тестов поднимается **отдельный изолированный контейнер PostgreSQL**, что гарантирует чистоту данных и безопасность основной базы разработки.

Запуск тестов:
```bash
make test
```

Конфигурация тестов находится в `docker-compose.test.yml` и `tests/conftest.py`.

---

## 📂 Структура проекта

```
.
├── .github/                        # CI/CD (GitHub Actions)
├── docker/                         # Dockerfiles и entrypoints
├── migrations/                     # Миграции базы данных (Alembic)
├── src/
│   ├── api/                        # Backend Application (FastAPI)
│   │   ├── core/                   # Конфиг, БД, Безопасность
│   │   ├── models/                 # SQLAlchemy модели
│   │   ├── repositories/           # Слой доступа к данным (CRUD)
│   │   ├── routes/                 # API Эндпоинты
│   │   ├── schemas/                # Pydantic схемы
│   │   ├── services/               # Бизнес-логика
│   │   ├── utils/                  # Вспомогательные утилиты
│   │   └── main.py                 # Запуск
│   │
│   ├── bot/                        # Telegram Bot (Aiogram)
│   │   ├── core/                   # Конфиг, перечисления (Enums)
│   │   ├── handlers/               # Обработчики команд и сообщений
│   │   ├── keyboards/              # Клавиатуры (Inline/Reply)
│   │   ├── services/               # Клиент API
│   │   ├── states/                 # FSM состояния
│   │   └── main.py                 # Запуск
│   │
│   ├── scheduler/                  # Планировщик задач (Apscheduler)
│   │   ├── config.py               # Конфиг
│   │   ├── tasks.py                # Логика уведомлений и обслуживания
│   │   └── main.py                 # Запуск
│   │
│   └── core_shared/                # Общий код
│       ├── logging_setup.py        # Настройка логирования
│       └── sentry_sdk_setup.py     # Настройка Sentry SDK
│
├── tests/                          # Тесты (Pytest)
│
├── .env.example                    # Пример файла конфигурации переменных окружения
├── docker-compose.yml              # Оркестрация
├── docker-compose.test.yml         # Оркестрация для тестов
├── Makefile                        # Утилиты запуска
├── poetry.lock                     # lock-файл версий зависимостей
└── pyproject.toml                  # Зависимости и настройки линтеров
```

---

## Автор

Developed by **Mikalai Sakalouski** with ❤️ and Python.
