"""
Модуль для генерации Reply-клавиатур (кнопок под строкой ввода).

Содержит функции для создания клавиатур главного меню и других навигационных элементов.
"""

from aiogram.types import KeyboardButton, ReplyKeyboardMarkup

# Константы текстов кнопок для использования в хендлерах
BTN_CREATE_HABIT = "➕ Создать привычку"
BTN_MY_HABITS = "📋 Мои привычки"
BTN_PROFILE = "👤 Профиль"


def get_main_menu_keyboard() -> ReplyKeyboardMarkup:
    """
    Генерирует клавиатуру главного меню бота.

    Структура меню:
    Row 1: [Создать привычку] [Мои привычки]
    Row 2: [Профиль]

    Returns:
        ReplyKeyboardMarkup: Объект клавиатуры, готовый к отправке пользователю.
    """
    keyboard = [
        [
            KeyboardButton(text=BTN_CREATE_HABIT),
            KeyboardButton(text=BTN_MY_HABITS),
        ],
        [
            KeyboardButton(text=BTN_PROFILE),
        ],
    ]

    return ReplyKeyboardMarkup(
        keyboard=keyboard,
        resize_keyboard=True,  # Делаем кнопки компактными, подстраиваясь под размер экрана
        input_field_placeholder="Выберите действие в меню 👇",  # Текст-подсказка в пустом поле ввода
    )
